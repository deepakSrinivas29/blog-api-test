server:
  port: 9500

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./blob.db

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true

